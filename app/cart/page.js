import { CartView } from "../components/cart-view";

export const metadata = {
  title: "Cart",
  description: "Review your selected medicines and continue checkout.",
};

export default function CartPage() {
  return <CartView />;
}
